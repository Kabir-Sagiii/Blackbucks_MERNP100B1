//Create a Function to add two numbers

function fnAdd(num1, num2, num3, x, y) {
  var x = num1;

  var y = num2;

  var result = x + y; //30 + 40
  console.log(result);
}

fnAdd(100, 200, 300, 4, 5, "cdkjb", true); //100 + 200

fnAdd(30, 40); // 30 + 40
